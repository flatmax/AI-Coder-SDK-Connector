"""Test suite for AIC-DC.

Tests are organized to mirror the src/aic_dc/ module layout. Each module's
unit tests live in tests/test_<module>.py. Integration tests that cross
module boundaries live in tests/integration/.
"""