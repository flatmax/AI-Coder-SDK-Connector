# Release — PyInstaller binaries on merge from a dev4-* branch → master.
#
# Triggered automatically when a pull request from a `dev4-*`
# development branch (e.g. dev4-agent, dev4-membrane) is merged
# into `master`. Also exposes `workflow_dispatch` for manual
# reruns. Never triggers on direct pushes to master or merges
# from other (non-dev4-) branches.
#
# Builds per-platform single-file binaries in parallel, then
# attaches all artefacts to a GitHub Release. The release tag on
# github.com is the computed version string (not a local git tag
# — no `git tag` / `git push --tags` steps).
#
# Version string format (specs-reference/6-deployment/build.md):
#     {YYYY.MM.DD}-{HH.MM}-{short_sha}
# Date/time UTC, short_sha is the first 8 chars of HEAD.
# Written to src/aic_dc/VERSION before PyInstaller runs so the
# baked binary self-identifies. Dev checkouts leave VERSION as
# the literal "dev".
#
# Docs extras split into two groups in pyproject.toml:
#   [docs-convert] — markitdown, pymupdf, python-pptx, openpyxl
#                    (small, ~60MB, bundled in releases)
#   [docs-enrich]  — keybert, sentence-transformers, torch
#                    (large, ~250MB+, NOT bundled; pip-install path)
#
# Users missing docs-enrich get a visible "install with
# pip install 'aic-dc[docs-enrich]'" toast per decision D1.

name: Release

on:
  pull_request:
    types: [closed]
    branches: [master]
  workflow_dispatch:

permissions:
  contents: write  # needed to create releases and attach artefacts

jobs:
  build:
    name: Build ${{ matrix.label }}
    # Gate: PR closed events only trigger the build when the PR
    # was actually merged (not just closed) AND came from a dev4-*
    # development branch (dev4-agent, dev4-membrane, ...). The
    # startsWith match means renaming the active dev branch no
    # longer silently disables releases.
    # workflow_dispatch bypasses the guard (manual trigger).
    if: |
      github.event_name == 'workflow_dispatch' ||
      (github.event.pull_request.merged == true &&
       startsWith(github.event.pull_request.head.ref, 'dev4-'))
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            label: linux
            binary-name: aic-dc-linux
            pyinstaller-sep: ":"
          - os: windows-latest
            label: windows
            binary-name: aic-dc-windows.exe
            pyinstaller-sep: ";"
          - os: macos-latest
            label: macos
            binary-name: aic-dc-macos
            pyinstaller-sep: ":"
    runs-on: ${{ matrix.os }}
    outputs:
      version: ${{ steps.version.outputs.version }}
    steps:
      - uses: actions/checkout@v4
        with:
          # Full history so `git rev-parse HEAD` returns the merge
          # commit's full SHA, not a shallow stub.
          fetch-depth: 0

      # uv (not pip): pip strictly enforces litellm's declared
      # `Requires-Python <3.14` and refuses to install 1.88 on
      # Python 3.14, whereas uv treats that upper cap as advisory.
      # uv also installs from uv.lock with `--frozen`, so release
      # builds are reproducible. Pinned to the uv version that
      # produced the lock.
      - uses: astral-sh/setup-uv@v5
        with:
          version: "0.11.16"
          python-version: "3.14"
          enable-cache: true

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: webapp/package-lock.json

      # Version string: YYYY.MM.DD-HH.MM-{short_sha}
      # UTC, no seconds, 8-char SHA. Written to VERSION so the
      # baked binary self-identifies. Emitted as a step output
      # for the release job to consume.
      - name: Compute version string
        id: version
        shell: bash
        run: |
          VERSION="$(date -u +%Y.%m.%d-%H.%M)-$(git rev-parse --short=8 HEAD)"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "$VERSION" > src/aic_dc/VERSION
          echo "Baked version: $VERSION"

      - name: Install webapp dependencies
        working-directory: webapp
        run: npm ci

      - name: Build webapp (Vite)
        working-directory: webapp
        run: npm run build

      # docs-convert only — no sentence-transformers/torch. Ships
      # markitdown, pymupdf, python-pptx, openpyxl so real users
      # get Word/PDF/PowerPoint/Excel conversion out of the box.
      # --frozen installs exactly what uv.lock pins (no re-resolve),
      # so every platform's binary is built from the same versions.
      - name: Install project with build + docs-convert extras
        run: uv sync --no-dev --extra build --extra docs-convert --frozen

      # PyInstaller command. Hidden-imports list enumerates every
      # aic_dc submodule; PyInstaller's static analysis misses
      # modules registered dynamically via add_class() or imported
      # by string. Belt-and-braces — extra entries cost nothing.
      - name: Build PyInstaller binary
        shell: bash
        run: |
          SEP="${{ matrix.pyinstaller-sep }}"
          # --no-sync: use the env from the previous `uv sync` step
          # verbatim; without it `uv run` re-syncs to the default
          # groups (re-adding dev, dropping the extras).
          uv run --no-sync pyinstaller --onefile \
            --name "${{ matrix.binary-name }}" \
            --add-data "src/aic_dc/VERSION${SEP}aic_dc" \
            --add-data "src/aic_dc/config${SEP}aic_dc/config" \
            --add-data "webapp/dist${SEP}aic_dc/webapp_dist" \
            --collect-all=litellm \
            --collect-all=tiktoken \
            --collect-all=tiktoken_ext \
            --collect-all=tree_sitter \
            --collect-all=tree_sitter_python \
            --collect-all=tree_sitter_javascript \
            --collect-all=tree_sitter_typescript \
            --collect-all=tree_sitter_c \
            --collect-all=tree_sitter_cpp \
            --collect-all=trafilatura \
            --collect-all=markitdown \
            --collect-all=fitz \
            --collect-all=pptx \
            --collect-all=openpyxl \
            --hidden-import=boto3 \
            --hidden-import=botocore \
            --hidden-import=jrpc_oo \
            --hidden-import=aic_dc \
            --hidden-import=aic_dc.base_cache \
            --hidden-import=aic_dc.base_formatter \
            --hidden-import=aic_dc.cli \
            --hidden-import=aic_dc.collab \
            --hidden-import=aic_dc.config \
            --hidden-import=aic_dc.context_manager \
            --hidden-import=aic_dc.doc_convert \
            --hidden-import=aic_dc.edit_pipeline \
            --hidden-import=aic_dc.edit_protocol \
            --hidden-import=aic_dc.file_context \
            --hidden-import=aic_dc.history_compactor \
            --hidden-import=aic_dc.history_store \
            --hidden-import=aic_dc.llm_service \
            --hidden-import=aic_dc.logging_setup \
            --hidden-import=aic_dc.main \
            --hidden-import=aic_dc.repo \
            --hidden-import=aic_dc.rpc \
            --hidden-import=aic_dc.settings \
            --hidden-import=aic_dc.stability_tracker \
            --hidden-import=aic_dc.token_counter \
            --hidden-import=aic_dc.doc_index \
            --hidden-import=aic_dc.doc_index.cache \
            --hidden-import=aic_dc.doc_index.formatter \
            --hidden-import=aic_dc.doc_index.index \
            --hidden-import=aic_dc.doc_index.keyword_enricher \
            --hidden-import=aic_dc.doc_index.models \
            --hidden-import=aic_dc.doc_index.reference_index \
            --hidden-import=aic_dc.doc_index.extractors \
            --hidden-import=aic_dc.doc_index.extractors.base \
            --hidden-import=aic_dc.doc_index.extractors.markdown \
            --hidden-import=aic_dc.doc_index.extractors.svg \
            --hidden-import=aic_dc.doc_index.extractors.svg_geometry \
            --hidden-import=aic_dc.symbol_index \
            --hidden-import=aic_dc.symbol_index.cache \
            --hidden-import=aic_dc.symbol_index.compact_format \
            --hidden-import=aic_dc.symbol_index.import_resolver \
            --hidden-import=aic_dc.symbol_index.index \
            --hidden-import=aic_dc.symbol_index.models \
            --hidden-import=aic_dc.symbol_index.parser \
            --hidden-import=aic_dc.symbol_index.reference_index \
            --hidden-import=aic_dc.symbol_index.extractors \
            --hidden-import=aic_dc.symbol_index.extractors.base \
            --hidden-import=aic_dc.symbol_index.extractors.c \
            --hidden-import=aic_dc.symbol_index.extractors.cpp \
            --hidden-import=aic_dc.symbol_index.extractors.javascript \
            --hidden-import=aic_dc.symbol_index.extractors.python \
            --hidden-import=aic_dc.symbol_index.extractors.typescript \
            --hidden-import=aic_dc.url_service \
            --hidden-import=aic_dc.url_service.cache \
            --hidden-import=aic_dc.url_service.detection \
            --hidden-import=aic_dc.url_service.fetchers \
            --hidden-import=aic_dc.url_service.models \
            --hidden-import=aic_dc.url_service.service \
            --hidden-import=aic_dc.url_service.summarizer \
            src/aic_dc/__main__.py

      - name: Upload artefact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.binary-name }}
          path: dist/${{ matrix.binary-name }}
          if-no-files-found: error
          retention-days: 7

  release:
    name: Publish GitHub Release
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download all artefacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: List artefacts
        run: find artifacts -type f -ls

      # Release notes: short description + per-platform download
      # table + optional-extras hint. Matches the style of the
      # previous runnable releases so existing users see a
      # familiar layout.
      - name: Generate release notes
        id: notes
        run: |
          VERSION="${{ needs.build.outputs.version }}"
          SHORT_SHA="$(echo "$VERSION" | rev | cut -d- -f1 | rev)"
          COMMITS_URL="https://github.com/${{ github.repository }}/commits/${SHORT_SHA}"
          cat > release_notes.md <<EOF
          ## AIC-DC ${VERSION}

          [View commit history](${COMMITS_URL})

          ### Downloads

          | Platform | File | Instructions |
          |----------|------|--------------|
          | Linux | \`aic-dc-linux\` | \`chmod +x aic-dc-linux && ./aic-dc-linux\` |
          | macOS | \`aic-dc-macos\` | \`chmod +x aic-dc-macos && ./aic-dc-macos\` |
          | Windows | \`aic-dc-windows.exe\` | Double-click or run from command prompt |

          ### Requirements

          - Run from within a git repository directory.

          ### Optional: keyword-enriched document mode

          Document mode works out of the box with heading outlines and
          cross-references. For keyword-enriched headings (better
          disambiguation when sections repeat), install from source:

          \`\`\`
          pip install "aic-dc[docs-enrich]"
          \`\`\`
          EOF

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.build.outputs.version }}
          name: ${{ needs.build.outputs.version }}
          body_path: release_notes.md
          files: artifacts/**/*
          fail_on_unmatched_files: true
          make_latest: true